name: dhikr_app
description: "تطبيق الذكر والتسبيح الإسلامي"
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2
  hijri: ^2.0.2
  flutter_qiblah: ^2.0.3
  geolocator: ^11.0.0
  permission_handler: ^11.3.0
  audioplayers: ^6.0.0
  share_plus: ^9.0.0
  url_launcher: ^6.2.5
  intl: ^0.19.0
  flutter_animate: ^4.5.0
  lottie: ^3.1.0
  wakelock_plus: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
    - assets/images/
    - assets/json/
